// Type Inference
// TypeScript infers the type of a variable based on the value it is assigned.
let drink = "Coffee";

let cups = Math.random() > 0.5 ? 10 : '5';

let myName = 'Zubair Sanaullah';

// Type Annotation
// Type annotation is a way to specify the type of a variable.
let coffeeType: string = 'Latte';
coffeeType = 'Cappuccino';

// coffeeType = 10; // Error: Type 'number' is not assignable to type 'string'.

let coffeeOrder:boolean

